import 'package:flutter/material.dart';

class CarsPageView extends StatelessWidget {
  const CarsPageView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Cars Management'));
  }
}