import 'package:flutter/material.dart';

class PaymentsPageView extends StatelessWidget {
  const PaymentsPageView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Payments Management'));
  }
}
